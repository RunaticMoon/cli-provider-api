[project]
name = "cli-provider-transports"
version = "0.1.0"
description = "Bounded NDJSON codec and stdio transport primitives."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cli_provider_transports"]
